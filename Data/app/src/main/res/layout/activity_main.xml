<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="420dp"
    android:layout_height="200dp" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/ink1"
    tools:context="com.mycompany.data.MainActivity">

    <TextView
        android:id="@+id/textView"
        android:text="@string/hello_world"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25dp"
        android:textStyle="bold"
        android:textColor="#ff000000" />

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:id="@+id/spinner"
        android:entries="@array/spinner_sensors"
        android:layout_alignParentRight="false"
        android:layout_alignParentEnd="false"
        android:background="#fff"
        android:layout_gravity="center"
        android:layout_centerInParent="true"
        android:dropDownSelector="@color/abc_background_cache_hint_selector_material_dark"
        android:focusable="true" />

</RelativeLayout>
